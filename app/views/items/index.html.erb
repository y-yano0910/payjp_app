<%= link_to 'マイページへ行く', user_path(current_user) %>
<div class="content">
  <% @items.each do |item| %>
    <p>商品名：<%= item.name %></p>
    <p>価格：<%= item.price %>円</p>
    <%= link_to '購入する' %>
  <% end %>
</div>